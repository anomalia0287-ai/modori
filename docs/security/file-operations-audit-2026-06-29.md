# File Operations Audit - 2026-06-29

Scope: production Python files under `src/` and developer/release scripts under `scripts/`
that call direct filesystem helpers:
`copyfile`, `copytree`, `scandir`, `shutil.rmtree`, `write_text`, `write_bytes`,
`mkdir`, `unlink`, `replace`, `rmdir`, `resolve`, or `lstat`, plus the Inno Setup
`[InstallDelete]` boundary.

Guard: `tests/test_file_operation_audit.py` fails when a new product file starts
using one of these operations without being added to this audited allowlist.

## Audited Files

| File | Operations | Boundary |
| --- | --- | --- |
| `scripts/live_research_os_office_benchmark.py` | `datetime.replace` | Pure developer benchmark contract for the frozen synthetic fixture, result schema, evaluation oracle, and canonical summary. The audit regex sees `datetime.replace` as a scanner false positive; this module performs no filesystem mutation, accepts no path authority, and is not packaged in the shipping app. |
| `scripts/live_research_os_office_kit.py` | `str.replace` | Pure canonical identity, manifest, package-lock, and naming contract shared by the dedicated builder and independent verifier. The audit regex sees `str.replace` as a scanner false positive; this module performs no filesystem mutation, accepts no path authority, and is not packaged in the shipping app. |
| `scripts/build_office_live_research_os_kit.py` | `lstat`, `mkdir`, `resolve`, `unlink`, `replace`, `shutil.rmtree` | Developer-only sealed benchmark builder. It requires a clean committed HEAD, fixed package lock and embedded runtime identity, and the dedicated `dist/live-research-os-office` output. Runtime and outer-kit materialization use exclusive creation below compact UUID-named staging roots; existing delivery files, links, reparse points, case collisions, unsafe members, source/bytecode contamination, dirty or mismatched commits, and outputs outside the dedicated directory are rejected. Cleanup removes only the identity-checked builder work root, verified staging root, or a just-created incomplete delivery hard link. It reads no user files and has no network client. |
| `scripts/run_office_live_research_os_benchmark.py` | `mkdir`, `resolve`, `write_bytes`, `unlink`, `replace`, `rmdir`, `shutil.rmtree` | Offline dedicated-kit runner over one frozen synthetic fixture. It accepts no caller-selected path: release mode derives the sibling `<extraction-parent>/w` and only `results` inside the kit, records marked ownership, creates closed run/child IDs, and performs a 240 UTF-16 code units dynamic path preflight before any child, fixture, or ledger work. Each persistent scenario receives a fresh marked child root; prior marked residues move only to `q/<nonce>`, and cleanup validates markers and exact ancestry before removing only marked child roots. Result JSON, digest, summary, and closed bootstrap errors are staged and published atomically below `results`. It touches no registry, reads no user dataset or document, is unreachable from the shipping app, and has no network client. |
| `scripts/verify_office_live_research_os_kit.py` | `lstat`, `resolve`, `scandir`, `mkdir` | Read-only inputs and fail-closed independent verification. ZIP mode verifies the explicit sidecar, sorted closed inventory, CRC, modes, path safety, traversal/device/ADS/case collisions, manifest bytes, identity, package lock, bootstrap chain, and every immutable digest before extracting with exclusive creation into an owned `TemporaryDirectory`; extracted-root mode uses no-follow inventory checks and accepts no sidecar. It launches only the sealed executable with two fixed identity arguments after static verification. The runtime identity probe uses a second verifier-owned `TemporaryDirectory` outside the supplied kit; both temporary scopes are context-owned cleanup, and the verifier does not modify the supplied ZIP or extracted root. |
| `scripts/benchmark_counterfactual_clarification.py` | `mkdir`, `write_text` | Developer-only formal-policy benchmark. It performs no dataset discovery and has no product entry point, network client, deletion, rename, or persistence integration. Without `--output` it writes canonical JSON to stdout only. With `--output`, it creates the caller-selected parent and overwrites exactly that caller-selected evidence file; repository practice uses ignored `.tmp/counterfactual-clarification-evidence.json`. The caller-selected overwrite is an explicit developer CLI boundary, not an operation reachable from user projects or the packaged product. |
| `scripts/benchmark_research_memory.py` | `shutil.rmtree` | Developer-only Decision Ledger benchmark. It creates a UUID-named directory below the worktree's existing `.test-tmp` root, prints metrics to stdout only, verifies the exact parent and `research-memory-benchmark-` prefix, and recursively removes only that owned temporary directory. It never runs in the product or writes benchmark evidence into the repository. |
| `scripts/capture_research_flow_gallery.py` | `mkdir`, `resolve`, `unlink`, `write_bytes` | Developer-only visual-evidence capture tool; it has no product entry point and is not packaged. It renders only synthetic application-content-only states from a frozen manifest, normally requires a clean committed source, binds every child render to the exact source commit, strips operating-system chrome and PNG text metadata, and writes only below a caller-selected empty output directory. It removes only the transient `.capture-<item-id>.json` record that the same run created after consuming it; gallery images and the final manifest are retained. The tool performs no user-data discovery, contains no user data, and has no network client. The explicit `--allow-dirty` option is a developer-test seam and does not make its outputs committed release evidence. |
| `scripts/build_research_flow_visual_review_packet.py` | `mkdir`, `resolve`, `write_text` | Developer-only fresh-session review-packet builder; it has no product entry point and is not packaged. It accepts a digest-bound rendered gallery plus its exact source manifest, requires a caller-selected empty output directory, verifies every source-image digest, re-encodes each PNG, rejects and strips text metadata, and writes only sanitized images, two fixed briefs, one contact sheet, and one packet manifest. The packet contains no source code, local paths, or user data, and the builder has no network client. It never deletes or overwrites an existing output. |
| `scripts/build_office_research_memory_kit.py` | `mkdir`, `resolve`, `unlink`, `shutil.rmtree` | Developer-only sealed-kit builder. It requires a clean committed source snapshot, an exact source allowlist, and the pinned official Python archive digest and inventory. It stages only below a UUID-named `.office-kit-build-` directory under the caller-selected `dist` root, refuses existing delivery files, finalizes by a same-volume hard link, and deletes only that verified staging directory or a just-created incomplete delivery hard link. It never copies `.git`, environments, fixtures, datasets, or user files and has no network client. |
| `scripts/run_office_research_memory_benchmark.py` | `resolve`, `shutil.rmtree` | Offline target-laptop runner. It verifies the kit before importing Modori, queries only bounded Windows hardware metadata, creates three prefix-checked synthetic benchmark directories below the kit's own `work` directory, and removes only each exact owned work directory. Final JSON, digest, and Korean summary use exclusive creation below the kit's `results` directory and never overwrite. It does not inspect user documents or use system temp. |
| `scripts/verify_office_research_memory_kit.py` | `resolve` | Read-only extracted-kit verifier. It walks only the kit's immutable tree without following links or entering `results`/`work`, hashes manifested files, and rejects missing, extra, linked, malformed, or modified content before payload import. It performs no mutation. |
| `scripts/verify_release_integration.py` | `mkdir`, `resolve`, `write_text` | Developer-only release-integration verifier. It invokes Git with argument lists and no shell to inventory two caller-selected commits, initialize a conflict ledger, and verify an exact two-parent merge. It creates the parent directory and overwrites exactly the caller-selected manifest, ledger, or verification JSON output; this explicit developer CLI boundary is not reachable from the packaged product or user projects. It has no network client, deletion, rename, dataset discovery, or persistence integration. |
| `src/modori/path_policy.py` | `resolve` | Central path validation helper. Rejects relative paths and symlink/junction ancestors before returning configured paths. |
| `src/modori/app.py` | `mkdir`, `write_text` | Hidden `--engine-smoke` and `--public-data-smoke` packaging gates only. The normal QML app path does not invoke these branches; the engine CLI calls the real application cache selector after its routed environment is active and includes the exact resolved cache path in its diagnostic JSON, while both CLIs write only to caller-supplied smoke output paths. |
| `src/modori/public_data_smoke.py` | `mkdir`, `write_text` | Hidden public-data release verification helper used only through `--public-data-smoke`. It reads checked-in or payload fixtures and writes one diagnostic JSON result to the caller-supplied smoke output path. |
| `src/modori/recommendation_baseline.py` | `resolve` | Read-only benchmark adapter. It resolves each case data path and rejects any path outside the selected pilot root before passing the file to the normal table importer. |
| `src/modori/recommendation_benchmark_io.py` | `mkdir`, `replace`, `unlink` | Central local benchmark I/O boundary. JSONL writes use same-directory random temporary files, `fsync`, and atomic replacement; overwrite requires an explicit flag. Workbook generation uses two owned temporary files and replaces only the caller-selected target. XML core properties are parsed with `defusedxml`; workbook loaders reject formulas, schema drift, missing case coverage, and invalid controlled vocabulary. Reviewer workbooks remain untrusted local inputs and may consume resources inside `openpyxl`, so they are not opened automatically by the product runtime. |
| `src/modori/research_memory/ledger_store.py` | `mkdir` | Decision Ledger persistence creates only the validated parent of one absolute local `.sqlite3` path. In the live Research OS flow, a closed local project ID selects a 32-hex SHA-256-derived project directory below `%LOCALAPPDATA%\Modori\projects`, with the fixed leaf `decision-ledger.sqlite3`; the live Research OS flow never accepts a caller-selected ledger path. The trusted lower-level store still validates its explicit path before mutation. UNC and mapped remote drives, relative paths, wrong suffixes, existing targets, and symlink/junction/reparse ancestry are rejected before creation and checked again afterward. SQLite owns the database/WAL files; this delivery exposes no clear, delete, replace, or arbitrary-path API. |
| `src/modori/research_memory/task_index.py` | `mkdir` | The non-authoritative Research OS task index creates only the validated parent of the fixed `%LOCALAPPDATA%\Modori\research-task-index.sqlite3` path. Its write inputs are closed identifiers, one lowercase SHA-256 dataset fingerprint, bounded ordinal/state values, and an explicit nullable UTC timestamp; it stores no raw value, source path, filename, URL, command, or free text. The managed-SQLite path proof runs before and after parent creation and rejects relative paths, UNC and mapped remote drives, wrong names or suffixes, and symlink/junction/reparse ancestry. SQLite uses WAL with full synchronous durability, a five-second busy timeout, defensive limits, and `BEGIN IMMEDIATE` transactions to serialize creation, allocation, replacement, and readonly transitions. Conflicts, unavailable resources, corruption, path failures, and runtime failures surface as typed conflict, unavailable, integrity, path, and runtime errors; unexpected SQLite failures roll back and the operation fails closed. The authorizer blocks schema change, attach/detach, row deletion, extension loading, and unauthorized writes. The public surface has no caller-selected path, no import, no delete, no bundle, no dynamic loader, and no network operation. A frozen 10,000-row resource cap prevents unbounded growth, but existing history is retained: task deletion and secure erasure are unavailable in this slice. |
| `src/modori/cache.py` | `resolve`, `mkdir`, `write_text`, `unlink` | Cache roots are selected through `resolve_secure_directory_path` where possible. `_ensure_cache_dir` creates only the selected cache directory, rejects link/junction targets, probes writability with a random temp filename, and deletes only that probe. |
| `src/modori/ui/chart_assets.py` | `mkdir`, `unlink` | Result-panel chart preview writes a random PNG filename only under the managed `cache/charts` directory. Failure cleanup deletes only that just-selected partial PNG path and returns a display note instead of touching user files. |
| `src/modori/ui/settings.py` | `resolve`, `mkdir`, `write_text`, `replace`, `unlink` | Settings path is either a secure absolute `.json` file or the managed cache. Writes go to a same-directory random temp file and then atomically replace the target; cleanup deletes only that temp path. |
| `src/modori/ui/result_binding.py` | `resolve`, `unlink` | Obsolete chart cleanup deletes only image files under the managed `cache/charts` directory after resolving both the cache root and candidate path. |
| `src/modori/ui/report_export.py` | `replace`, `unlink` | After the trusted report exporter has generated and returned an existing output path, disclosure post-processing moves that file to a random same-directory temporary path, edits only the staged copy, and atomically publishes it back. If staging or disclosure fails, cleanup removes only that just-generated output and its random sibling so an unlabeled assisted report is not left at the published path. The production exporter routes through the trusted `ReportStep`; untrusted project JSON cannot instantiate that step, and injected exporters are controller test seams rather than user-selected plugins. |
| `src/modori/ui/session.py` | `resolve` | Recent-file storage records a resolved path string only; it does not read, write, or delete the referenced file. |
| `src/modori/ui/worker.py` | `resolve`, `write_text` | Engine exception traces are written only when explicitly enabled by `MODORI_DEBUG_ENGINE_ERRORS=1` or an executable-adjacent `enable-engine-debug` sentinel. The file is written under the managed Modori cache directory and is not part of the normal user-facing error path. |
| `src/modori/steps/reporting.py` | `mkdir`, `resolve`, `unlink`, `rmdir` | `ReportStep` is not allowed in untrusted project JSON. Report paths reject direct output aliases, path separators in filenames, symlink output targets, and chart dirs outside `output_dir`. Failure cleanup deletes only created report/chart files under `output_dir` and removes the output directory only when this run created it. |
| `src/modori/knowledge/loader.py` | `resolve` | Read-only package data root resolution. No mutation. |
| `src/modori/ui/resources.py` | `resolve` | Read-only QML resource root resolution. No mutation. |
| `scripts/build_installer.py` | `copyfile`, `lstat`, `mkdir`, `replace`, `resolve`, `scandir`, `write_bytes` | Before creating a run root or snapshot file, the builder validates `WORKSPACE`, `.tmp`, and `.tmp/ib` component-by-component with `lstat` without following links, rejecting every symlink/junction/reparse point, non-directory, or resolved escape; it creates missing components one at a time and revalidates the unique run root after exclusive creation. At frozen-input entry after package-build return, and again immediately before snapshot creation, it revalidates the supplied staging ancestry component-by-component without following links so replacement during the package build cannot redirect a snapshot write. It then creates a deterministic full-tree snapshot and exact installer script copy inside one unique compact `.tmp/ib/<commit12>-<uuid12>` staging tree, using fixed children including `s/p`, `s/modori.iss`, `so`, `po`, `dp`, `do`, and `c`; an existing run name raises without altering its content. The lexical workspace-to-package boundary and every descendant are enumerated by non-following `scandir`, reject any link or junction/reparse point, and require resolved containment before hash/copy traversal. Relative paths, sizes, and content SHA values are verified before and after copying. Before any compiler output or ISCC call, the authenticated frozen inventory and resolved snapshot root compute the actual compiler source maximum in strict UTF-16 code units across files, directories, and directory search wildcards (including root and directory `\*`) and fail closed above `240`; an inventory with no files also fails closed. Package smokes and the smoke/production compilers use the frozen payload, while a staging-owned tiny downgrade payload contains only its sentinel `Modori.exe` and uses the same frozen script. The selected compiler must equal registered `InstallLocation\ISCC.exe`; its registered version, actual fixed file version, and uppercase SHA256 are recorded separately and re-read before and after every ISCC invocation. The builder, after candidate materialization, requires exactly three regular non-reparse candidate files, validates their sizes/SHA256 plus manifest/checksum relationships after the final source/toolchain check, and repeats that validation before and after each publication move. Bare invocation is rejected before mutation; build-only mode is staging-only and cannot publish. Publication requires successful installed lifecycle verification, captures staging and destination ancestry identities, requires the same volume, moves first to a unique hidden sibling, revalidates it, and only then promotes it to the immutable canonical `dist/installer/<build-id>` path. A failed validation moves only the tracked candidate back to staging without touching a pre-existing or concurrently appearing final directory; diagnostic staging evidence is preserved on failure. It does not delete user data. |
| `scripts/capture_entry_states.py` | `mkdir`, `resolve`, `unlink` | Developer-only design QA harness. It creates a caller-selected evidence directory, writes only six fixed-name entry captures, fixed-name comparison PNGs when a source reference is supplied, and one temporary `.capture-settings.json` in that same directory. The settings file contains only checked-in fixture paths and is removed in a `finally` block; the harness never reads or changes the user's normal Modori settings or recent-file storage. It does not delete user data. |
| `scripts/capture_work_states.py` | `mkdir`, `resolve`, `unlink` | Developer-only design QA harness. It creates a caller-selected evidence directory, writes only six fixed-name work-screen captures, fixed-name comparison PNGs when both approved references are supplied, and one temporary `.capture-settings.json` in that same directory. The harness opens only the checked-in `psych_bfi.csv` fixture, removes only its temporary settings file in a `finally` block, and never reads or changes the user's normal Modori settings, recent-file storage, data, or reports. It does not delete user data. |
| `scripts/installer_contract.py` | `write_text` | The contract measures source, payload-path, installer, and SHA256 evidence, then writes `release-manifest.json`, the smoke manifest, or `SHA256SUMS.txt` only to caller-selected staging paths. It does not select or delete user paths. |
| `scripts/installer_smoke.py` | `lstat`, `mkdir`, `resolve`, `rmdir`, `unlink`, `write_bytes`, `write_text` | Lifecycle smoke validates `WORKSPACE/.tmp/installer-smoke` component-by-component before mutation, exclusively creates `r-<12-hex>`, captures ancestor/run identities, and revalidates them before every installer, package-smoke, repair, downgrade, uninstall, and cleanup boundary. It routes installed package cache, settings, and Matplotlib state into the direct child `user-state`. After bounded polling proves the registration, shortcut, and entire install root are absent, recursive cleanup freezes every descendant identity, rejects each symlink/junction/reparse entry, and revalidates run root, user-state, and each entry before and after bottom-up deletion. Engine evidence is retained as exact regular `engine-smoke/reference.xlsx` plus `result.json`, and public-data evidence as exact `public-data-smoke/result.json`. Each inventory is repeated after reads, sizes/SHA256 are frozen, shared wrapper contracts are applied, and exact bytes are revalidated after uninstall and cleanup; sibling logs and evidence remain preserved. |
| `installer/modori.iss` | `[InstallDelete]` | Repair cleanup deletes only the installer-owned `{app}\Modori` payload tree. It never targets product user state in `%LocalAppData%\Modori\cache`; that cache remains outside the per-user install tree and outside this delete boundary. |
| `scripts/package_environment.py` | `lstat`, `mkdir`, `resolve` | Shared release-only environment boundary. It removes workspace R runtime directories from packaged build/run `PATH` values so R DLLs cannot contaminate the PySide6 package. The lifecycle creates each unique test-owned state/evidence root and supplies an absolute lexical path. Paths are validated component-by-component with no-follow `lstat`, rejecting every symlink/junction/reparse point; `ExplicitDirectoryBoundary` additionally freezes every evidence-directory ancestor identity and can require an empty directory or a contained regular child before/after wrapper operations. Cache, Matplotlib, and settings remain below the validated state root, and installed runtime children are not precreated. Standalone smokes retain isolated workspace `.tmp` setup. It does not select or clean product user data. |
| `scripts/package_windows.py` | `mkdir` | Local release-build setup under the workspace `.tmp` directory. Not user-data cleanup. |
| `scripts/package_launch_smoke.py` | `mkdir`, `resolve` | Local smoke-test setup under the workspace `.tmp` directory and explicit executable/cwd normalization. Not product runtime deletion. |
| `scripts/package_engine_smoke.py` | `copyfile`, `mkdir`, `resolve`, `unlink` | Default host smoke uses workspace `.tmp/packaged-engine-smoke`, creates its deterministic workbook, and deletes only its own old result. Lifecycle mode instead requires a pre-existing empty identity-bound evidence directory, writes workbook/result under unpredictable transient names, validates regular contained files, then promotes them to exact `reference.xlsx`/`result.json` names without deleting prior content. It copies the verified workbook into a unique identity-bound direct child of the test state root before launching the app, so report sidecars such as `modori-output` remain disposable test state and cannot pollute durable evidence. The shared engine payload contract requires the reported cache path to equal routed `MODORI_CACHE_DIR`, successful statistics evidence, and a real directory that is not a link/junction. It does not delete user data. |
| `scripts/package_public_data_smoke.py` | `mkdir`, `resolve`, `unlink` | Default host smoke uses workspace `.tmp/packaged-public-data-smoke` and deletes only its own old result. Lifecycle mode requires a pre-existing empty identity-bound evidence directory, writes to an unpredictable transient result, validates it as a contained regular file with the shared hardened public-data contract, and promotes it to exact `result.json` without deleting prior content. It does not delete user data. |
| `scripts/stress_matrix.py` | `mkdir`, `write_text` | Local stress evidence generation under caller-selected output paths; release checklist uses ignored `.stress-matrix`. It creates deterministic synthetic datasets, JSON results, and report artifacts, not user-data cleanup. |
| `scripts/quality_gate.py` | `resolve` | Local release-gate environment setup only. It resolves the workspace-local `.tools/r-env/Scripts/Rscript.exe` path for pytest and slow statistical references, while package and ordinary command environments strip that runtime through `package_environment.py`. It does not create, modify, or delete files. |
| `scripts/recommendation_benchmark.py` | `resolve` | Developer-only benchmark scoring CLI. It resolves the repository root and reads the frozen scorer core, workbook I/O, and CLI sources to bind score output to an implementation digest. Source discovery never runs inside the packaged product scorer core and performs no mutation. |
| `scripts/build_recommendation_pilot.py` | `mkdir`, `write_text`, `write_bytes`, `resolve` | Developer-only deterministic fixture builder. It writes a fixed set of benchmark files below the caller-selected root, refuses existing outputs unless `--force` is explicit, validates data paths against root escape, and validates the completed pack before reporting success. Interruption can leave an incomplete pack, which the validator rejects; this script is not a product runtime or user-data cleanup path. |
| `scripts/check_product_wording.py` | `resolve` | Read-only release scanner. It resolves the selected repository root, reads UTF-8 product text only from fixed source, help, README, and `docs/product` paths, and excludes an explicit historical benchmark allowlist. It does not create, modify, or delete files. |

## Decisions

- No new delete-capable product file operation is accepted without updating this
  audit and extending tests around its boundary.
- Installer repair cleanup remains limited to `{app}\Modori`; the application
  cache at `%LocalAppData%\Modori\cache` is user state and is never an
  `[InstallDelete]` target.
- `ReportStep`, table imports, and regression imports remain blocked from
  untrusted project JSON, so attacker-controlled project files cannot trigger
  report output writes or cleanup.
- Direct helper calls such as `render_chart` and `write_docx` are treated as
  trusted internal/reporting API entry points. User-facing project loading must
  go through `Pipeline.from_json`, which rejects those IO steps unless trusted.
- Research OS task-index and ledger records are retained. `decision_retracted`
  changes authority state but is not deletion; whole-task deletion, delete-all,
  and forensic secure erasure remain deferred lifecycle work.

## Residual Risk

- Filesystem race conditions between path validation and write/delete operations
  are reduced but not eliminated. The current product stance is local desktop
  hardening, not a multi-tenant sandbox guarantee.
- Relative report `output_dir` remains allowed for trusted workflows and resolves
  against the current working directory. Untrusted project JSON cannot invoke it.
