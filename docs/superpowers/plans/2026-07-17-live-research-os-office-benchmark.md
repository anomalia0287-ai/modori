# Live Research OS Office Benchmark Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. The owner forbids subagents, so execute inline with a fresh review checkpoint after every task.

**Goal:** Seal and run an offline, synthetic, independently verifiable Windows kit that measures the complete live Research OS waits on the same low-cost HP office laptop, enforces the approved 30-second p95 and 100-millisecond UI-acknowledgement gates without averaging stages together, and produces the final evidence-backed go/stop disposition.

**Architecture:** Build a dedicated PyInstaller one-folder benchmark executable from the committed product authority chain; do not add a benchmark command to the shipping app. A parent runner launches only its own verified child executable with a fixed argument list for cold-process samples and runs a fixed warm series in-process. Each child overrides `LOCALAPPDATA` to a verified disposable application-owned benchmark root on the internal disk, so it exercises the production path policy without touching a user's Modori ledger. A deterministic outer ZIP, sidecar, embedded bootstrap digests, closed manifest, canonical result schema, and independent result verifier surround that runtime.

**Tech Stack:** Python 3.12.10; existing PyInstaller Windows toolchain; PySide6/QCoreApplication; the committed Research OS/Research Memory/Research Flow/calculation code; canonical JSON and SHA-256; PowerShell 5.1-compatible bootstrap; pytest; Ruff; Bandit.

## Entry conditions and non-negotiable constraints

- Execute only after every task in `2026-07-17-live-multi-round-research-os-ui.md` passes at a clean committed HEAD. Freeze that exact HEAD as `source_commit` everywhere in the kit.
- Work only in `C:\Users\V\Desktop\TongTong\.worktrees\research-os-release-integration`. Do not modify either source worktree.
- Do not switch branches, merge, push, open a pull request, delete a worktree, or spend GitHub Actions quota.
- Do not use subagents. Inspect each diff and local result directly.
- Use synthetic data only. No user file, recent item, filename, path, project title, free-text answer, ledger, or actual research record enters the kit or result.
- No network request, telemetry, package download, installer, service, registry mutation, elevation, arbitrary command, or arbitrary path.
- Do not modify or relabel the previous `modori-office-benchmark-kit-cc1e94b...` research-memory kit. This is a new schema, runner, kit name, and claim.
- The target acceptance rows are separate: initial identity wait, initial decision round, later decision round, and main-thread acknowledgement. A combined average is forbidden.
- The fingerprint worker retains its approved `10_000 ms` internal deadline. A
  fingerprint timeout is a failed run even if the broader identity screen would have
  remained under 30 seconds.
- Use nearest-rank p95 over raw observations. Do not trim outliers, discard slow valid runs, substitute means, pool cold and warm samples, or pool a fast profile over a slow profile.
- Each profile stratum and each overall stage must meet the threshold. The acceptance ceiling is `30_000 ms` p95 for each visible wait and `100 ms` p95 for acknowledgement.
- CPU, RAM, storage, peak working set, and I/O are reported. There is no invented memory pass threshold; process failure, OS resource failure, or incomplete measurement fails the run.
- No threshold, repetition count, fixture size, stage boundary, or path condition may be changed after seeing the target result.
- This kit tests execution feasibility and wait time. It does not prove recommendation validity, numerical accuracy, expert equivalence, SPSS superiority, or coverage beyond the six capabilities.

## Frozen measurement protocol

### Acceptance fixture

The deterministic fixture is `modori.live_research_os.office_fixture.v1`:

- 50,000 ordered rows by 40 ordered columns = 2,000,000 cells;
- 16 SCALE numeric columns, including independent outcome, paired before/after,
  Pearson pair, and additional finite/missing variants;
- 8 ORDINAL columns, including the Spearman pair;
- 8 NOMINAL columns, including an exactly two-level group;
- 4 Boolean columns and 2 date columns with stable typed encodings;
- 2 NFC-normalized text columns used only to exercise fingerprint typing;
- deterministic missing masks, finite values, nonzero variances, adequate group sizes,
  paired differences, variable metadata, and source-schema descriptor; and
- no random seed, file import, or wall-clock value. Values are generated by fixed
  index-based formulas and the complete fixture digest is asserted.

This size is frozen before target measurement because it represents a demanding but
bounded wide social-science table, exercises every supported profile in one identity,
and stays materially below the separate 5,000,000-cell resource ceiling.

All six profiles use fixed variable IDs from this one fixture. A separate 5,000,000-cell
stress fixture checks cancellation/resource behaviour but is explicitly non-acceptance:
it may reveal a limit, but cannot rescue or fail the frozen 2,000,000-cell result.

### Repetitions and strata

- **Cold series:** 20 fresh identity-only child processes plus 20 fresh scenario child
  processes for **each** of the six profiles (140 fresh processes total). Each child
  builds the fixture before the timer. Identity children take one full identity sample;
  each scenario child walks only its designated profile through a fresh task and every
  required later answer round, so no profile is made artificially warm by its position
  after another profile.
- **Warm series:** 30 complete in-process iterations after one untimed warm-up. Every
  iteration performs an uncached full fingerprint for the identity row, then fresh task
  allocation/initial decision and later rounds while reusing only the exact fingerprint
  where the product contract permits it.
- Every profile therefore has at least 20 cold and 30 warm observations for initial
  decision. Every actual later round is reported by profile and round ordinal; the
  overall later-row p95 includes all valid later-round observations, and acceptance also
  requires every profile/round p95 to pass.
- Main-thread acknowledgement is measured for every public P1 action in both cold and
  warm series from call entry until the busy/static state signal is observable. Worker
  completion is not part of this row.
- Human reading/answer time, fixture generation, process import/startup, result
  serialization, and cleanup are outside the timed waits and reported separately.

### Result contract

The canonical result uses schema ID `modori.live_research_os_office_benchmark`, version
1, and contains only:

```text
run_id
recorded_at_utc | null
source_commit
kit_identity_digest
protocol_digest
fixture identity, digest, dimensions, and declared column-kind counts
closed execution-condition evidence
coarse non-identifying hardware/OS/storage evidence
all raw duration observations in integer nanoseconds
per-series/profile/round nearest-rank p95 values
main-thread acknowledgement observations
peak working set and bounded resource observations
closed error/reason codes
recomputed evaluation
result_hash
```

`result_hash` is SHA-256 over the canonical result object with `result_hash` omitted.
The `.json.sha256` sidecar hashes the final canonical JSON bytes. The Korean summary is
generated only from the verified parsed result and is not an authority source. Hardware
evidence excludes hostname, username, device/volume serial, MAC/IP, exact user path,
installed-app inventory, and file names.

`run_id` is a fresh closed UUID used only to bind child observations and output names.
`recorded_at_utc` is optional display metadata from an injected UTC clock; neither field
changes thresholds, ordering, authority, or evaluation, and local-time fallback is
forbidden.

## File map

### Benchmark contract and runner

- Create `scripts/live_research_os_office_benchmark.py`: fixture, protocol, timing,
  evaluation, result canonicalization, and bounded hardware/path probes.
- Create `scripts/run_office_live_research_os_benchmark.py`: trusted parent/child CLI,
  fixed process orchestration, isolated `LOCALAPPDATA`, output, and summary.
- Create `tests/test_live_research_os_office_benchmark_contract.py`.
- Create `tests/test_live_research_os_office_benchmark_runner.py`.

### Sealed runtime and kit

- Create `scripts/live_research_os_office_kit.py`: shared manifest, identity, and verifier
  contracts with no imports from the builder.
- Create `scripts/build_office_live_research_os_kit.py`.
- Create `scripts/verify_office_live_research_os_kit.py`.
- Create `scripts/office_live_research_os_kit/RUN-MODORI-LIVE-RESEARCH-OS-BENCHMARK.cmd.in`.
- Create `scripts/office_live_research_os_kit/VERIFY-AND-RUN.ps1.in`.
- Create `scripts/office_live_research_os_kit/README-KO.txt`.
- Create `tests/test_office_live_research_os_kit_contract.py`.
- Create `tests/test_office_live_research_os_kit_builder.py`.
- Create `tests/test_office_live_research_os_kit_verifier.py`.
- Create `tests/test_office_live_research_os_kit_runner.py`.
- Create `tests/test_office_live_research_os_kit_architecture.py`.

### Runbook and final evidence

- Create `docs/qa/live-research-os-office-benchmark-runbook.md`.
- Create after a valid returned target result only: `docs/qa/live-research-os-p1-evidence.md`.

---

### Task B1: Freeze the fixture, stage boundaries, and evaluation oracle

**Files:**

- Create: `scripts/live_research_os_office_benchmark.py`
- Create: `tests/test_live_research_os_office_benchmark_contract.py`

**Interfaces:**

```python
@dataclass(frozen=True)
class OfficeBenchmarkProtocol:
    cold_processes_per_stratum: int = 20
    warm_iterations: int = 30
    fingerprint_worker_limit_ms: int = 10_000
    visible_wait_limit_ms: int = 30_000
    acknowledgement_limit_ms: int = 100


@dataclass(frozen=True)
class BenchmarkFixture:
    dataset: Dataset
    source_schema: SourceSchemaDescriptor
    profile_drafts: tuple[tuple[P1TaskProfile, P1IntakeDraft], ...]
    fixture_digest: str
    row_count: int
    column_count: int


@dataclass(frozen=True)
class EvaluationRow:
    stage: str
    cache_state: str
    profile: str | None
    round_ordinal: int | None
    sample_count: int
    p95_ns: int
    maximum_ns: int
    limit_ns: int
    passed: bool


@dataclass(frozen=True)
class BenchmarkEvaluation:
    disposition: Literal["pass", "stop"]
    reason_codes: tuple[str, ...]
    rows: tuple[EvaluationRow, ...]


def build_acceptance_fixture() -> BenchmarkFixture: ...
def nearest_rank_p95(samples_ns: Sequence[int]) -> int: ...
def evaluate_result(result_without_evaluation: Mapping[str, object]) -> BenchmarkEvaluation: ...
def canonical_result_bytes(result: Mapping[str, object]) -> bytes: ...
```

- [x] **Step 1: Write exact fixture identity tests**

Assert dimensions, column order/kinds, metadata, typed missingness, the fixed digest, all
six role bindings, and feasibility floors. Repeated builds must produce identical
dataset/source-schema fingerprints and values. A row reorder, column reorder, metadata
change, missing-mask change, NFC mutation, bool/int collision, float/date mutation, or
source-layout change must alter the appropriate identity.

- [x] **Step 2: Write p95 and no-averaging tests**

Use known 20- and 30-value sequences to prove the exact nearest-rank index. Reject too
few, nonpositive, Boolean, float, missing, reordered-without-identity, or undeclared
samples. Construct a result where the pooled p95 passes while one profile fails and
require overall failure. Construct cold-pass/warm-fail, identity-pass/decision-fail, and
round-1-pass/round-2-fail cases; each must fail independently.

- [x] **Step 3: Write canonical result and privacy tests**

Require the closed top-level/nested field sets, canonical key order/number profile,
`result_hash` recomputation, source/protocol/fixture digest binding, and sidecar bytes.
Reject NaN/Infinity, unknown fields, trusted stored `pass`, missing raw observations,
hostname, username, serials, MAC/IP, absolute paths, filenames, free text, user values,
and a summary that does not reproduce the verified evaluation.

- [x] **Step 4: Run the tests and confirm failure**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_live_research_os_office_benchmark_contract.py -q -p no:cacheprovider
```

Expected: FAIL because the benchmark contract is absent.

- [x] **Step 5: Implement the closed contract and fixture**

Use `time.perf_counter_ns()` only through an injected timer. Keep fixture generation
outside timed spans. Recompute evaluation from raw samples and frozen thresholds every
time; never accept a serialized evaluation as proof. Keep the 5,000,000-cell stress
fixture under a separate explicit API and result section with no acceptance influence.

- [x] **Step 6: Run contract tests**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_live_research_os_office_benchmark_contract.py tests/test_research_flow_fingerprint.py tests/test_research_flow_handoff.py tests/test_research_flow_preflight.py -q -p no:cacheprovider
```

Expected: all pass.

- [x] **Step 7: Commit**

```powershell
git add -- scripts/live_research_os_office_benchmark.py tests/test_live_research_os_office_benchmark_contract.py
git commit -m "test: freeze live Research OS office benchmark"
```

Execution evidence on 2026-07-18: the first focused run failed at collection because
the benchmark contract module did not exist. After the first implementation, the RED
set exposed the three deliberately unfrozen identity constants plus two defective test
examples: one attempted to replace a cell that was already missing, and the pooling
example slowed an entire profile so both the profile and pooled p95 failed. The examples
were corrected without changing any gate. The 2,000,000-cell fixture was then frozen at
dataset fingerprint
`3b499392e8e1764359b43d45d9c965a4ae759bda89e1d2df0647c51158551c2d`, source-schema
fingerprint
`e3ff262b4a6829f3acdae909e2caeecd2910688e8e3d444a943b5a77c2079e60`, and fixture
digest `f0a70250178dbf4a2a59795b72356d6c1dcaa1046e0ce4dada04885d5814602b`.
The separate 5,000,000-cell stress fixture was frozen at digest
`3dad9993f2e0114ec310436e3df6d02381ff696404a5947022c0cdbca14fdc82` and is proven
unable to change acceptance. The focused contract file passed `46` tests; the contract
plus fingerprint, handoff, and preflight regression cohort passed `202` tests. Ruff
check/format, Bandit, source compilation, and `git diff --check`
passed with cache disabled after the sandboxed formatter was unable to write this
external dedicated worktree's cache. No threshold, repetition, fixture size, or stage
boundary changed.

### Task B2: Exercise the real committed authority chain in cold and warm series

**Files:**

- Create: `scripts/run_office_live_research_os_benchmark.py`
- Create: `tests/test_live_research_os_office_benchmark_runner.py`
- Modify: `scripts/live_research_os_office_benchmark.py`

**Interfaces:**

```python
@dataclass(frozen=True)
class ChildObservation:
    run_id: str
    child_id: str
    cache_state: Literal["cold", "warm"]
    profile: P1TaskProfile | None
    identity_wait_ns: int | None
    initial_decision_ns: int | None
    later_decision_ns: tuple[tuple[int, int], ...]
    acknowledgement_ns: tuple[tuple[str, int], ...]
    peak_working_set_bytes: int


def run_child_scenario(
    protocol: OfficeBenchmarkProtocol,
    *,
    source_commit: str,
    isolated_local_app_data: Path,
) -> ChildObservation: ...

def run_complete_benchmark(
    protocol: OfficeBenchmarkProtocol,
    *,
    verified_self_executable: Path,
    working_root: Path,
) -> Mapping[str, object]: ...
```

- [ ] **Step 1: Write real-flow timing-boundary tests**

Instrument the actual fingerprint worker/function, task index, ledger creation/open,
initial append, resolver/planner, passport append/audit, handoff, preflight, projection,
and answer transition. Assert the three timer spans include exactly the components in
design Section 14. Fixture construction, child process startup/import, serialization,
and cleanup must be separately timed and excluded. A later round must reuse only the
exact cached fingerprint and still include every later authority operation.

- [ ] **Step 2: Write six-profile walk and no-shortcut tests**

Every warm iteration must execute all six approved profiles with the exact safe answer
walk; each cold scenario child executes its one declared profile. Every walk ends in the
intended unique capability and exact preparation. Poison legacy
recommendations, fake passports, schema-only fingerprints, pre-seeded ledgers,
preflight bypass, planner-result reuse across tasks, and an unverified route. The runner
must fail rather than measure a shortcut.

- [ ] **Step 3: Write parent/child isolation and process tests**

The parent invokes only its own already verified executable, with `shell=False`, a fixed
closed argument list, bounded timeout, and per-child nonce/root. It sets child
`LOCALAPPDATA` to `%LOCALAPPDATA%\ModoriBenchmarkRuns\<run-id>\LocalAppData` after
proving the root is local, internal, regular, non-reparse, and application-owned. Reject
USB/network/cloud/reparse roots, inherited `PYTHONPATH`, arbitrary executable/argument,
extra environment variables, child schema/digest mismatch, nonzero exit, timeout,
partial output, or duplicate child identity.

- [ ] **Step 4: Write crash, acknowledgement, and cleanup tests**

Crash before/after each product append and parent collection boundary. Reopen and verify
the last committed synthetic task; do not count a failed scenario as a duration sample.
Measure controller action-to-busy/static signal without running the worker on the main
thread. Cleanup may remove only the exact disposable synthetic benchmark run root after
result sealing; it is not exported as a product ledger-delete API. A crash residue is
detected and quarantined on the next run, never merged into samples.

- [ ] **Step 5: Run the tests and confirm failure**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_live_research_os_office_benchmark_runner.py tests/test_live_research_os_office_benchmark_contract.py -q -p no:cacheprovider
```

Expected: FAIL because the runner is absent.

- [ ] **Step 6: Implement fixed orchestration and output**

Generate one canonical JSON, `.json.sha256`, and `.summary-ko.txt` only after all child
observations verify. Before that point, failures create a timestamped
`bootstrap-error-*.txt` with a closed reason code and no partial success JSON. Keep raw
samples in JSON; the summary reports every cold/warm/stage/profile/round p95 and never a
combined average.

- [ ] **Step 7: Run runner and authority regression tests**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_live_research_os_office_benchmark_runner.py tests/test_live_research_os_office_benchmark_contract.py tests/test_research_flow_coordinator.py tests/test_research_flow_concurrency.py tests/test_research_memory_crash_recovery.py tests/ui/test_research_flow_controller.py -q -p no:cacheprovider
```

Expected: all pass using reduced test-only repetition counts that cannot be selected by
the release CLI. The release protocol remains hard-coded to 20/30.

- [ ] **Step 8: Commit**

```powershell
git add -- scripts/run_office_live_research_os_benchmark.py scripts/live_research_os_office_benchmark.py tests/test_live_research_os_office_benchmark_runner.py
git commit -m "test: run full live Research OS office protocol"
```

### Task B3: Build a dedicated sealed one-folder runtime and deterministic outer kit

**Files:**

- Create: `scripts/live_research_os_office_kit.py`
- Create: `scripts/build_office_live_research_os_kit.py`
- Create: `scripts/office_live_research_os_kit/RUN-MODORI-LIVE-RESEARCH-OS-BENCHMARK.cmd.in`
- Create: `scripts/office_live_research_os_kit/VERIFY-AND-RUN.ps1.in`
- Create: `scripts/office_live_research_os_kit/README-KO.txt`
- Create: `tests/test_office_live_research_os_kit_contract.py`
- Create: `tests/test_office_live_research_os_kit_builder.py`
- Modify: `tests/test_package_environment.py`

**Kit name:**

`modori-live-research-os-office-kit-<source_commit[0:12]>-py31210.zip`

The ZIP contains one identically named root directory. It is written only under
`dist/live-research-os-office/`, so prior benchmark artifacts are neither deleted nor
relabeled.

- [ ] **Step 1: Write the closed identity and manifest tests**

`KIT-IDENTITY.json` must bind schema/version, full source commit, protocol and fixture
digests, Python/SQLite/PySide versions, PyInstaller version/bootloader digest, package-
lock digest, executable relative path, builder/verifier contract versions, and expected
result schema. `MANIFEST.json` contains sorted unique normalized relative paths, byte
lengths, and SHA-256 for every immutable identity/runtime/payload file. Reject unknown
identity fields, noncanonical bytes, duplicate/case-colliding/absolute/traversal/ADS
paths, backslashes, links/reparse points, devices, extra/missing files, and unsupported
versions.

- [ ] **Step 2: Write the bootstrap-chain and PowerShell 5.1 tests**

The CMD template contains and verifies the exact PowerShell bootstrap digest before
launch. PowerShell contains and verifies the exact manifest and identity digests before
the benchmark executable starts. The manifest verifies the executable, bootloader,
DLLs, Python modules, Qt runtime, and payload. Outer ZIP digest covers the CMD itself.
Require literal argument arrays, no download/network/elevation/registry/service/power-
plan change, no encoded command, no wildcard execution, and no arbitrary executable.

- [ ] **Step 3: Write dedicated-runtime closure tests**

Build a PyInstaller **one-folder console** executable whose entry point is only
`run_office_live_research_os_benchmark.py`. Assert it imports the exact committed
Research OS/Memory/Flow, calculation validators, pandas/numpy/PySide dependencies, and
no source-worktree module at runtime. It must self-report source/protocol/runtime
identity and fail if these differ from `KIT-IDENTITY.json`. The shipping `Modori.exe`
gains no benchmark switch, and the shipping package gains no benchmark fixture or CLI.

- [ ] **Step 4: Run the tests and confirm failure**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_office_live_research_os_kit_contract.py tests/test_office_live_research_os_kit_builder.py tests/test_package_environment.py -q -p no:cacheprovider
```

Expected: FAIL because the separate kit contract/builder is absent.

- [ ] **Step 5: Implement a clean-HEAD builder**

The builder refuses a dirty worktree, detached/mismatched source commit, missing lock,
unsupported runtime, unexpected PyInstaller warning, or output outside the dedicated
directory. Build the executable once into an immutable staging tree, then create the
manifest/identity/bootstrap chain. Normalize ZIP member order, timestamps, modes, UTF-8
flags, and compression. Repacking one fixed staged runtime three times must be byte-
identical; do **not** claim that independently rebuilt PE/PyInstaller binaries are
reproducible unless separately proven.

Pass explicit `--workpath`, `--distpath`, and `--specpath` under the exact
`.tmp/live-research-os-office-kit/` root so this build cannot overwrite `dist/Modori` or
another kit. The selected outer artifact alone is copied to
`dist/live-research-os-office/`.

- [ ] **Step 6: Run builder and package-separation tests**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_office_live_research_os_kit_contract.py tests/test_office_live_research_os_kit_builder.py tests/test_package_environment.py tests/test_package_windows_script.py tests/test_package_public_data_smoke_script.py -q -p no:cacheprovider
```

Expected: all pass without changing the production package inventory beyond product
files already authorized by the core plan.

- [ ] **Step 7: Commit**

```powershell
git add -- scripts/live_research_os_office_kit.py scripts/build_office_live_research_os_kit.py scripts/office_live_research_os_kit/RUN-MODORI-LIVE-RESEARCH-OS-BENCHMARK.cmd.in scripts/office_live_research_os_kit/VERIFY-AND-RUN.ps1.in scripts/office_live_research_os_kit/README-KO.txt tests/test_office_live_research_os_kit_contract.py tests/test_office_live_research_os_kit_builder.py tests/test_package_environment.py
git commit -m "build: seal live Research OS office kit"
```

### Task B4: Independently verify, attack, and locally smoke the final kit

**Files:**

- Create: `scripts/verify_office_live_research_os_kit.py`
- Create: `tests/test_office_live_research_os_kit_verifier.py`
- Create: `tests/test_office_live_research_os_kit_runner.py`
- Create: `tests/test_office_live_research_os_kit_architecture.py`
- Create: `docs/qa/live-research-os-office-benchmark-runbook.md`

- [ ] **Step 1: Write an independent verifier test**

The verifier imports only `live_research_os_office_kit.py`, never the builder, and
accepts either a ZIP+sidecar or extracted root. It independently checks outer SHA-256,
ZIP CRC, sorted/unique complete entries, traversal/link/reparse/extra-file absence,
identity canonicalization, every manifest size/digest, bootstrap embedded digests,
runtime self-identity, source commit, protocol/fixture digest, and executable closure.
It must complete before any child or output JSON can run.

- [ ] **Step 2: Write returned-result verification tests**

Given a kit identity plus JSON/sidecar/summary, independently verify canonical bytes,
file SHA-256, `result_hash`, raw observation counts/types, child identities, stage/profile/
round completeness, nearest-rank p95 recomputation, evaluation recomputation, execution
conditions, hardware schema, source/kit/protocol/fixture binding, and summary equality.
Reject a stored `pass` when recomputation fails. The API returns typed `valid_pass`,
`valid_stop`, or `invalid_run`; invalid protocol is not mislabeled as product failure.

- [ ] **Step 3: Write a 300-case mutation and path corpus**

Cover one-byte changes across executable/bootloader/DLL/Python/Qt/payload/identity/
manifest/PowerShell/CMD/ZIP, truncation, missing/extra/duplicate/case-colliding members,
CRC damage, manifest reorder, identity/source/protocol mutation, ZIP traversal, ADS,
absolute/device names, nested ZIP, symlink/junction/reparse roots, USB/network/cloud
locations, arbitrary child args/env, stale sidecar, result/summary mutation, observation
deletion/reorder, p95/evaluation forgery, and contamination with source/bytecode/work
files. Every immutable mutation blocks before a result JSON; every returned-result
mutation is rejected by independent recomputation.

- [ ] **Step 4: Write the precise Korean runbook and error contract**

The runbook names the exact ZIP/sidecar pattern, local target directory, required AC
power, fresh extraction, expected long-running progress, output file names, and returned
files. It says explicitly: do not run from USB, OneDrive, Google Drive, another synced
folder, or a reparse point; do not modify any unrelated partition/folder. The only files
to return are JSON, JSON sidecar, Korean summary, and—on failure—the bootstrap-error file
plus optional photo. It distinguishes rerunnable setup error from a valid threshold
stop.

- [ ] **Step 5: Run the tests and confirm failure**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_office_live_research_os_kit_verifier.py tests/test_office_live_research_os_kit_runner.py tests/test_office_live_research_os_kit_architecture.py tests/test_office_live_research_os_kit_contract.py tests/test_office_live_research_os_kit_builder.py -q -p no:cacheprovider
```

Expected: FAIL because the independent verifier, attack corpus, and runbook are absent.

- [ ] **Step 6: Implement the verifier and fixed bootstrap**

Use handles/byte streams already verified; do not verify one path and execute a second
unresolved path. PowerShell probes AC state and working-volume/path conditions with a
closed schema, records coarse hardware evidence, and invokes only the verified runner.
The benchmark may run on an internal HDD or SSD; media class is reported, not used to
weaken the single 30-second threshold.

- [ ] **Step 7: Commit verifier, attacks, and runbook**

Run the focused tests above to green, then:

```powershell
git add -- scripts/verify_office_live_research_os_kit.py tests/test_office_live_research_os_kit_verifier.py tests/test_office_live_research_os_kit_runner.py tests/test_office_live_research_os_kit_architecture.py docs/qa/live-research-os-office-benchmark-runbook.md
git commit -m "test: verify live Research OS office kit"
```

- [ ] **Step 8: Build three identical outer archives at committed HEAD**

Build one immutable runtime staging tree, repack it independently three times, and
compare bytes. Preserve only the selected final ZIP and `.zip.sha256` under the dedicated
dist directory after verifying all three. Record full path, size, SHA-256, member count,
CRC, duplicates, identity, runtime versions, and source commit. Do not delete or modify
older research-memory kit artifacts elsewhere.

- [ ] **Step 9: Attack the selected archive and run the full protocol locally**

Execute all 300 mutations against disposable extracted copies. Then run the unmodified
kit from a fresh local non-reparse directory on the development PC using the full 20/30
release protocol. Verify the returned files independently. Report the development
result as smoke/performance context only; it cannot pass or replace the HP gate.

- [ ] **Step 10: Re-run the complete product quality gate**

Run:

```powershell
.\.venv\Scripts\python.exe scripts/quality_gate.py --with-package-check --with-package-build --with-packaged-launch --with-slow-stats
```

Expected: all local product and package gates remain green at the exact kit source
commit. If kit work changes product behaviour or package contents unexpectedly, stop
instead of shipping the measurement tool.

### Task B5: Run the target HP protocol and issue the final go/stop evidence

**Files:**

- Create after valid target verification: `docs/qa/live-research-os-p1-evidence.md`
- Modify only on a valid stop: the narrow UI exposure/configuration files required to
  disable the live Research OS entry while preserving direct analysis and legacy
  experimental candidates
- Modify only after a measured performance defect: the smallest product files proven by
  profiling and their regression tests

- [ ] **Step 1: Freeze and report the exact target kit**

Before transfer, independently verify the final ZIP and sidecar again and report their
absolute development-PC paths, full file names, byte size, SHA-256, source commit, and
expected target extraction directory:

`%LOCALAPPDATA%\ModoriBench-LiveResearchOS-<source_commit[0:12]>`

No similarly named prior kit is valid evidence.

- [ ] **Step 2: Execute on the same low-cost HP laptop**

With AC connected, copy ZIP+sidecar from USB to the exact local directory, create a
fresh ordinary local extraction there, and run
`RUN-MODORI-LIVE-RESEARCH-OS-BENCHMARK.cmd`. Do not run from USB or a synced/reparse
folder. Let the fixed cold/warm protocol finish; the console may show stage/profile/
iteration progress but no provisional pass. Return:

```text
modori-live-research-os-office-benchmark-<run-id>.json
modori-live-research-os-office-benchmark-<run-id>.json.sha256
modori-live-research-os-office-benchmark-<run-id>.summary-ko.txt
```

If it fails before those files, return `bootstrap-error-*.txt` and an optional photo.

- [ ] **Step 3: Classify protocol validity before product performance**

Run the independent verifier against the original kit identity and returned files.
Reject checksum/canonical/result-hash failure, source/protocol/fixture mismatch,
observation gaps, condition failure, battery execution, USB/cloud/reparse working root,
wrong runtime, contamination, forged evaluation/summary, or non-release repetition
counts as `invalid_run`. Correct a reasonable setup error and repeat; do not count it as
a product pass or stop.

- [ ] **Step 4: Recompute every target gate**

For a valid run, report separately:

- cold and warm initial-identity p95;
- cold and warm initial-decision p95 overall and for all six profiles;
- cold and warm later-decision p95 overall, per profile, and per round;
- acknowledgement p95 for each public P1 action;
- raw sample counts, maximums, peak working set, CPU/RAM/storage, and execution
  conditions; and
- any closed failure/resource code.

Every required subgroup must satisfy 30,000 ms and every acknowledgement subgroup must
satisfy 100 ms. Do not pool rows, profiles, rounds, or cache states.

- [ ] **Step 5: Permit one evidence-backed optimization cycle, not threshold tuning**

If a valid target row fails, use its component spans to identify one bottleneck. Add a
failing performance/regression test, make the smallest safe optimization, run the full
product plan gates, commit, rebuild a newly named/source-pinned kit, repeat all local
attacks/smoke, and rerun the complete HP protocol. The old result cannot validate the
new commit. Do not reduce fixture/repetitions, move work outside the timer, alter cache
rules, drop samples, or raise thresholds.

- [ ] **Step 6: Apply the stop rule if the valid failure persists**

If any wait or acknowledgement gate still fails after that cycle—or any authority,
integrity, privacy, crash, accessibility, or package gate fails—classify the live
passport-backed P1 surface `stop`. Disable its product entry point with a failing test
first; preserve direct analysis, the separately labelled legacy experimental candidates,
and the deterministic Research OS research code/evidence. Re-run the complete local
quality/package gate. Do not introduce an SLM or a looser uncommitted imitation.

- [ ] **Step 7: Write final evidence for either outcome**

Create `docs/qa/live-research-os-p1-evidence.md` with:

- measured source commit and final documentation commit distinction;
- kit/result hashes and independent-verifier result;
- exact fixture/protocol and all separate target metrics;
- local full-suite/package/mutation evidence;
- go or stop reasoning tied to frozen gates;
- task-history destruction still unavailable;
- claims and nonclaims from design Section 19; and
- any warning, skip, resource increase, limitation, or invalid rerun.

A go permits only the experimental, local, six-task, no-auto-run claim. A stop records
the evidence and disabled surface; it is not rewritten as success.

- [ ] **Step 8: Verify and commit the final evidence state**

Run the documentation, boundary, focused P1, and complete local gates at the final
source state. Confirm a clean diff except the evidence document (and, for stop, the
tested disable delta). Then stage exact paths and commit with either:

```powershell
git add -- docs/qa/live-research-os-p1-evidence.md
git commit -m "docs: record live Research OS P1 evidence"
```

or the exact evidence plus tested disable paths for a stop. Never stage returned raw
hardware JSON, USB files, `dist`, `.visual-qa`, temp ledgers, or unrelated changes.

---

## Final decision semantics

- **Go:** every local/product/kit/mutation gate passes and every valid HP subgroup meets
  the frozen timing/acknowledgement threshold. The UI remains experimental and never
  auto-runs.
- **Invalid run:** transport, checksum, path, power, runtime, schema, observation, or
  protocol evidence is invalid. Fix only the rational setup cause and rerun unchanged.
- **Stop:** a valid target or product gate remains failed after one focused repair. Keep
  the deterministic core and direct analysis, but do not expose the live P1 path.

Passing this plan still establishes no human gold, recommendation accuracy, statistical
validity, numerical equivalence, causal validity, general SPSS superiority, or secure
erasure.

## Execution handoff

Execute inline with `superpowers:executing-plans`; subagents are forbidden. Review
`git diff --check`, exact staged paths, focused output, and worktree status after every
task. Never use `git add -A`. Stop whenever an approved gate cannot be met without
changing its meaning.
